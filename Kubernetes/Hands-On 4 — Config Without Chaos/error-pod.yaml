apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: team-a
spec:
  containers:
    - name: app
      image: busybox
      command: ["sh", "-c", "sleep 3600"]
      env:
        - name: MISSING
          valueFrom:
            configMapKeyRef:
              name: does-not-exist
              key: something
