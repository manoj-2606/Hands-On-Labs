apiVersion: batch/v1
kind: Job
metadata:
  name: hello-job
  namespace: batch-lab
spec:
  template:
    spec:
      containers:
        - name: worker
          image: busybox
          command: ["echo", "Job completed successfully"]
      restartPolicy: Never
